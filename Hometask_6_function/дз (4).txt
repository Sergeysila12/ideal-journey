написать функцию, которая примет два целых числа (границы диапазона), а вернёт случайное число из этого диапазона


------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
#include <iostream>
#include <cmath>

//сигнатура
//[тип] [имя] (параметры) 
//{тело функции}
//
int function(int a, int b)
{
    return a + b;
    //std::cout << "функция вызвана";
}

int power(int number, int degree)
{
    int res = 1;
    for (int i=0; i < degree; i++)
    {
        res = res * number;
    }
    return res;
}

int main()
{
    /*int height;
    std::cin >> height;
    for (int i = 0; i < height; i++)
    {
        for (int j = 0; j < height - i - 1; j++)
        {
            std::cout << " ";
        }
        for (int s = 0; s < i * 2 + 1; s++)
        {
            std::cout << "*";
        }
        std::cout << std::endl;
    }*/
    //0 1 2 3 4 5
    //if (6 % 2 == 0)
        //(arr[size/2] + arr[size/2-1]) / 2
    //else
        //arr[size/2]
    /*int start = 1;
    int end = 10;

    for (int number = start; number <= end; number++)
    {
        bool isPrime = true;
        for (int i = 2; i <= std::sqrt(number); i++)
        {
            if (number % i == 0)
            {
                isPrime = false;
                break;
            }
        }
        if (isPrime)
        {
            std::cout << number;
        }
    }*/
    /*const int SIZE = 10;
    int arr[SIZE] = { 5, 10, 3, 0, 11, 3, 1, 8, 2, 9 };*/

    //for (int i=0; i < SIZE - 1; i++)
    //{
    //    bool isSwaped = false;
    //    for (int j=0; j < SIZE - i - 1; j++)
    //    {
    //        if (arr[j] > arr[j + 1])
    //        {
    //            int temp = arr[j];
    //            arr[j] = arr[j + 1];
    //            arr[j + 1] = temp;
    //            //std::swap(arr[j], arr[j + 1]);
    //            isSwaped = true;
    //        }
    //    }
    //    if (!isSwaped) break;
    //}

    /*int arr2d[SIZE / 5][SIZE / 5] = 
    {
        {0, 0},
        {3, 4}
    };*/
    
    //std::cout << arr2d[0][1];
    setlocale(LC_ALL, "ru");
    //вызов функции
    //int res = function(5, 4);
    //std::cout << function(10, 3);
    
    int num;
    int deg;
    std::cin >> num;
    std::cin >> deg;

    std::cout << power(num, deg);
}
